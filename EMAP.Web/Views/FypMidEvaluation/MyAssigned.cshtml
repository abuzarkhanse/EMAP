@model IEnumerable<EMAP.Domain.Fyp.FypEvaluation>

@{
    ViewData["Title"] = "My Mid Evaluations";
}

<h2 class="mb-4">My Mid Evaluations</h2>

<div class="card shadow-sm border-0">
    <div class="card-body">
        <table class="table table-hover align-middle">
            <thead>
                <tr>
                    <th>Group</th>
                    <th>Program</th>
                    <th>Batch</th>
                    <th>Scheduled</th>
                    <th>Venue</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.StudentGroup?.TentativeProjectTitle</td>
                        <td>@item.StudentGroup?.ProgramCode</td>
                        <td>@item.StudentGroup?.FypCall?.Batch</td>
                        <td>@(item.ScheduledAt?.ToString("dd MMM yyyy hh:mm tt") ?? "-")</td>
                        <td>@item.Venue</td>
                        <td>
                            @if (item.IsSubmitted)
                            {
                                <span class="badge bg-success">Submitted</span>
                            }
                            else
                            {
                                <span class="badge bg-warning text-dark">Pending</span>
                            }
                        </td>
                        <td>
                            <a asp-action="Evaluate" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Evaluate</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>